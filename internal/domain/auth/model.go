package auth

import (
	"errandShop/internal/pkg/models"
)

// User type alias for the shared model
type User = models.User

// Address type alias for the shared model
type Address = models.Address

// RefreshToken type alias for the shared model
type RefreshToken = models.RefreshToken

// OTP type alias for the shared model
type OTP = models.OTP
